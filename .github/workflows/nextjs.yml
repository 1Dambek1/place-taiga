name: Disabled legacy Pages workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Notice
        run: echo "Legacy workflow disabled. Use .github/workflows/deploy.yml"
